# buf.yaml
version: v2

modules:
  - path: api          # твои .proto
  - path: third_party  # vendor: google/*, openapi/*, validate/*

deps: []               # лишних deps не нужно

lint:
  use: [DEFAULT]
breaking:
  use: [FILE]